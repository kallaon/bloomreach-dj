export enum CompareTypeString {
  EQUALS = 'equals',
  DOES_NOT_EQUAL = 'does_not_equal',
  CONTAINS = 'contains',
  DOES_NOT_CONTAIN = 'does_not_contain',
}

export enum CompareTypeNumber {
  EQUALS_TO = 'equals_to',
  IN_BETWEEN = 'in_between',
  LESS_THAN = 'less_than',
  GREATER_THAN = 'greater_than',
}

export enum CompareType {
  STRING = 'string',
  NUMBER = 'number',
}
